App.Coup.Character.duke: App.Coup.Character
  let name = "Duke"
  let img = []
  let actions = [App.Coup.CharacterAction.tax, App.Coup.CharacterAction.block_foreign_aid]
  App.Coup.Character.new(name, img, actions)
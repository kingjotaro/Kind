Shape.Rectangle.to_polygon(rectangle: Shape.Rectangle): Shape.Polygon
  let x0 = rectangle@a@x
  let x1 = rectangle@b@x
  let y0 = rectangle@a@y
  let y1 = rectangle@b@y
  let a = V2.new(x0, y0)
  let b = V2.new(x0, y1)
  let c = V2.new(x1, y1)
  let d = V2.new(x1, y0)
  [a,b,c,d]